import { redirect } from "next/navigation"

export default function SubmitPage() {
  // Redirect to an external form (e.g., Google Forms, Tally)
  // Replacing with a placeholder URL for now.
  redirect("https://tally.so/r/placeholder")
}
